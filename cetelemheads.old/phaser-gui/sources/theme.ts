module phasergui {
  export class Theme {
    border: number = 2;

    buttonBackgroundColor: string = '#fff';
    buttonBorderColor: string = '#fff';

    buttonBackgroundHoverColor: string = '#eee';
    buttonBorderHoverColor: string = '#fff';

    buttonBackgroundDisabledColor: string = '';
    buttonBorderDisabledColor: string = '';

    buttonBackgroundDownColor: string = '#aaa';
    buttonBorderDownColor: string = '#fff';

    checkboxMarkColor: string = '#000';
    radioMarkColor: string = '#000';
  }
}
